class Solution(object):
    def runningSum(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        arrayAccumulated = len(nums)
        accumt = 0
        runningSum = []
        for numIndex in range(len(nums)):
            accumt  = accumt + nums[numIndex]
            runningSum[numIndex]
        return 
            
